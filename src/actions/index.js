import { 
   POSTS_REQUESTED, 
   POSTS_LOADED, 
   POSTS_REJECTED, 
   ADD_POST, 
   POST_LIKED,
   FRIENDS_REQUESTED,
   FRIENDS_LOADED,
   FRIENDS_REJECTED 
} from './types';

import { 
   postsRequested, 
   postsLoaded, 
   postsRejected, 
   addedPost, 
   uploadPosts,
   friendsRequested,
	friendsLoaded,
	friendsRejected,
	uploadFriends
} from './actionCreators';


export { 
   POSTS_REQUESTED, 
   POSTS_LOADED, 
   POSTS_REJECTED, 
   ADD_POST, 
   POST_LIKED, 
   FRIENDS_REQUESTED,
   FRIENDS_LOADED,
   FRIENDS_REJECTED
};
export { 
   postsRequested, 
   postsLoaded, 
   postsRejected, 
   addedPost, 
   uploadPosts,
   friendsRequested,
	friendsLoaded,
	friendsRejected,
	uploadFriends 
};